---
phase: 04-design-output
plan: 02
type: execute
wave: 1
depends_on: [04-01]
files_modified:
  - components/card-news/design/DesignTokenSystem.tsx
autonomous: true
requirements:
  - DSGN-04
user_setup: []
must_haves:
  truths:
    - "디자인 토큰이 상태로 관리된다"
    - "CSS 변수로 디자인 토큰이 적용된다"
    - "색상 없을 경우 기본 팔레트가 사용된다"
    - "폰트 추출 실패 시 시스템 폰트가 사용된다"
    - "디자인 토큰 변경이 상태에 반영된다"
  artifacts:
    - path: components/card-news/design/DesignTokenSystem.tsx
      provides: Hook for managing design token state with CSS variable application
      exports: ["useDesignTokenSystem", "createDesignTokenState"]
  key_links:
    - from: DesignTokenSystem.tsx
      to: components/lib/types.ts
      via: DesignToken type import
      pattern: "from '@/components/lib/types'"
    - from: useDesignTokenSystem hook
      to: CSS custom properties
      via: getCardStyle function return value
      pattern: "--color-primary.*--color-secondary.*--font-family"
---

<objective>
디자인 토큰 상태를 관리하는 훅을 생성하고 CSS 변수를 통해 카드 컴포넌트에 디자인 토큰을 적용한다.

Purpose: 레퍼런스 이미지 분석 결과를 받아 CSS 변수 형태의 디자인 토큰으로 변환하고, 이를 카드 컴포넌트에 적용하여 일관된 디자인 시스템을 구축한다.
Output: useDesignTokenSystem hook for managing design token state
</objective>

<execution_context>
@/Users/gim-yohan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gim-yohan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-design-output/04-CONTEXT.md
@.planning/phases/04-design-output/04-RESEARCH.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md

# Existing patterns from previous phases
@.planning/phases/03-structure-review/03-01-SUMMARY.md
@.planning/phases/02-quality-verification/02-02-SUMMARY.md
@.planning/phases/01-research-copy-generation/01-02-SUMMARY.md
</context>

<interfaces>
<!-- Key types and contracts from codebase -->

From components/lib/types.ts (added in 04-01):
```typescript
export interface DesignToken {
  primaryColor: string; // e.g., "#3b82f6"
  secondaryColor: string;
  accentColor: string;
  fontCategory: 'sans-serif' | 'serif' | 'mono';
  layoutPattern: 'minimal' | 'bold' | 'layered' | 'centered';
  mood: 'professional' | 'playful' | 'serious' | 'elegant';
  backgroundColor: string | 'transparent';
}

export function getDefaultDesignToken(): DesignToken;
export function getCardStyle(token: DesignToken): React.CSSProperties;
```

From components/card-news/quality/EvaluationSystem.tsx (hook pattern):
```typescript
export function useEvaluationSystem() {
  const [state, setState] = useState<EvaluationState>(createInitialEvaluationState());

  const startEvaluation = useCallback(() => {
    setState((prev) => ({
      ...prev,
      phase: 'evaluating',
      isEvaluating: true,
      error: null,
    }));
  }, []);

  const recordEvaluationResult = useCallback((evaluations: AgentEvaluation[]) => {
    setState((prev) => ({
      ...prev,
      history: [...prev.history, { loop: prev.loopCount, evaluations }],
      isEvaluating: false,
    }));
  }, []);

  return { state, startEvaluation, recordEvaluationResult, ... };
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create DesignTokenSystem hook</name>
  <files>components/card-news/design/DesignTokenSystem.tsx</files>
  <action>Create DesignTokenSystem.tsx with useDesignTokenSystem hook:

```typescript
'use client';

import { useState, useCallback } from 'react';
import type { DesignToken, getDefaultDesignToken } from '@/components/lib/types';

/**
 * Design token state phases
 */
export type DesignTokenPhase =
  | 'idle'
  | 'uploading'
  | 'analyzing'
  | 'extracted'
  | 'error';

/**
 * Design token state
 */
export interface DesignTokenState {
  phase: DesignTokenPhase;
  token: DesignToken;
  referenceImage: string | null;
  error: string | null;
}

/**
 * Initial state factory
 */
export function createInitialDesignTokenState(): DesignTokenState {
  return {
    phase: 'idle',
    token: getDefaultDesignToken(),
    referenceImage: null,
    error: null,
  };
}

/**
 * Hook for managing design token state
 * DSGN-04: 분석 결과를 디자인 토큰(JSON)으로 변환해 각 카드에 자동 적용한다
 * DSGN-04: CSS 변수로 디자인 토큰 적용 (재사용 가능, 유지보수 용이)
 */
export function useDesignTokenSystem() {
  const [state, setState] = useState<DesignTokenState>(createInitialDesignTokenState());

  /**
   * Set reference image and transition to uploading phase
   */
  const setReferenceImage = useCallback((base64: string | null) => {
    setState((prev) => ({
      ...prev,
      referenceImage: base64,
      phase: base64 ? 'analyzing' : 'idle',
      error: null,
    }));
  }, []);

  /**
   * Set extracted design token
   */
  const setDesignToken = useCallback((token: DesignToken) => {
    setState((prev) => ({
      ...prev,
      token,
      phase: 'extracted',
    }));
  }, []);

  /**
   * Reset to initial state
   */
  const reset = useCallback(() => {
    setState(createInitialDesignTokenState());
  }, []);

  /**
   * Set error state
   */
  const setError = useCallback((error: string) => {
    setState((prev) => ({
      ...prev,
      phase: 'error',
      error,
    }));
  }, []);

  return {
    state,
    setReferenceImage,
    setDesignToken,
    reset,
    setError,
  };
}
```

This hook follows the same pattern as EvaluationSystem.tsx: state machine, useCallback actions, initial state factory.</action>
  <verify>ls -la components/card-news/design/DesignTokenSystem.tsx && grep -n "export function useDesignTokenSystem" components/card-news/design/DesignTokenSystem.tsx && grep -n "export function createInitialDesignTokenState" components/card-news/design/DesignTokenSystem.tsx</verify>
  <done>useDesignTokenSystem hook manages design token state with phase transitions</done>
</task>

</tasks>

<verification>
1. useDesignTokenSystem hook is created with proper state machine
2. State includes phase, token, referenceImage, and error
3. All actions use useCallback for memoization
4. Initial state factory provides default design token
5. Reset action returns to idle state with default token
6. TypeScript compilation passes with no errors
</verification>

<success_criteria>
1. DesignTokenSystem.tsx is created with useDesignTokenSystem hook
2. Hook provides state management for design tokens
3. Actions (setReferenceImage, setDesignToken, reset, setError) are properly memoized
4. Design token type is correctly imported from types.ts
5. Default design token is used for initial state (DSGN-04 기본 팔레트 사용)
</success_criteria>

<output>
After completion, create `.planning/phases/04-design-output/04-02-SUMMARY.md`
</output>

---
phase: 01-research-copy-generation
plan: 03
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - components/card-news/ResearchResults.tsx
  - components/card-news/CardNewsList.tsx
  - app/card-news/page.tsx
autonomous: true
requirements:
  - RSCH-03
  - RSCH-04
  - RSCH-05
  - RSCH-06
user_setup: []
must_haves:
  truths:
    - "Research sources are displayed in an expandable accordion"
    - "Each source shows title, summary, and link to original"
    - "Card news cards are displayed in a list view"
    - "Each card shows type badge, headline, and subtext"
    - "Cover card is first, body cards in middle, CTA card is last"
    - "User can edit card text directly in the list"
  artifacts:
    - path: "components/card-news/ResearchResults.tsx"
      provides: "Research sources display using Accordion component"
      exports: ["ResearchResults"]
    - path: "components/card-news/CardNewsList.tsx"
      provides: "Card news list display with editable cards"
      exports: ["CardNewsList"]
  key_links:
    - from: "components/card-news/ResearchResults.tsx"
      to: "components/lib/types.ts"
      via: "ResearchSource type import"
      pattern: "import.*ResearchSource.*from.*@/components/lib/types"
    - from: "components/card-news/CardNewsList.tsx"
      to: "components/lib/types.ts"
      via: "CardNewsItem, CardNewsResponse type imports"
      pattern: "import.*CardNews.*from.*@/components/lib/types"
    - from: "app/card-news/page.tsx"
      to: "components/card-news/ResearchResults.tsx"
      via: "Component import and data passing"
      pattern: "import.*ResearchResults.*from.*@/components/card-news/ResearchResults"
    - from: "app/card-news/page.tsx"
      to: "components/card-news/CardNewsList.tsx"
      via: "Component import and data passing"
      pattern: "import.*CardNewsList.*from.*@/components/card-news/CardNewsList"
---

<objective>
Create research results display and card news list components with editable functionality.

Purpose: Build the results display layer - show research sources in accordion format and display generated card news in an editable list view.
Output: ResearchResults component with Accordion UI, CardNewsList component with editable cards, integrated into main page.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-research-copy-generation/01-CONTEXT.md
@.planning/phases/01-research-copy-generation/01-RESEARCH.md
@.planning/phases/01-research-copy-generation/01-01-PLAN.md
@.planning/phases/01-research-copy-generation/01-02-PLAN.md
</context>

<interfaces>
<!-- Key types from 01-01-PLAN.md that this plan consumes -->

From components/lib/types.ts:
```typescript
export interface ResearchSource {
  title: string;
  url: string;
  summary: string;
}

export interface CardNewsItem {
  type: 'cover' | 'body' | 'cta';
  headline: string;
  subtext: string;
  order: number;
}

export interface CardNewsResponse {
  cards: CardNewsItem[];
  researchSources: ResearchSource[];
}
```

<!-- Existing shadcn/ui components to use -->

From components/ui/accordion.tsx:
```typescript
export {
  Accordion,
  AccordionItem,
  AccordionTrigger,
  AccordionContent,
} from "@/components/ui/accordion"
// AccordionItem props: value, children
// AccordionTrigger: children, className
// AccordionContent: children, className
```

From components/ui/card.tsx:
```typescript
export {
  Card,
  CardHeader,
  CardTitle,
  CardContent,
} from "@/components/ui/card"
// Card: children, className
// CardHeader: children, className
// CardTitle: children, className
// CardContent: children, className
```

From components/ui/badge.tsx:
```typescript
export { Badge } from "@/components/ui/badge"
// Badge props: variant (default, secondary, outline, destructive), children, className
```

From components/ui/textarea.tsx:
```typescript
export { Textarea } from "@/components/ui/textarea"
// Textarea props: className, placeholder, value, onChange, disabled
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create ResearchResults component</name>
  <files>components/card-news/ResearchResults.tsx</files>
  <action>Create components/card-news/ResearchResults.tsx:

1. 'use client' directive

2. Component signature: ResearchResults({ sources }: { sources: ResearchSource[] })

3. Import types: ResearchSource from '@/components/lib/types'
4. Import components: Accordion, AccordionItem, AccordionTrigger, AccordionContent, Badge from '@/components/ui'

5. Render:
   - Section header with title "리서치 결과"
   - Accordion component with type="multiple" and className="w-full"
   - Map over sources array, for each source (with index):
     - AccordionItem with value={`source-${index}`}
     - AccordionTrigger with className="hover:no-underline":
       - Badge with variant="secondary" and text "출처 {index + 1}"
       - Source title in a span with font-medium
     - AccordionContent with className="space-y-2 pt-2":
       - Summary paragraph with text-sm text-muted-foreground
       - Link to original URL with:
         - href={source.url}
         - target="_blank"
         - rel="noopener noreferrer"
         - className="text-sm text-blue-600 hover:underline"
         - text "원문 보기"

6. Handle empty sources: Display message "리서치 결과가 없습니다" if sources array is empty

This component displays research sources in an expandable accordion format per user decision.</action>
  <verify>File exists at components/card-news/ResearchResults.tsx with ResearchResults component. Run: grep -E "export.*function ResearchResults|export const ResearchResults" components/card-news/ResearchResults.tsx</verify>
  <done>ResearchResults component created with Accordion display for research sources</done>
</task>

<task type="auto">
  <name>Task 2: Create CardNewsList component</name>
  <files>components/card-news/CardNewsList.tsx</files>
  <action>Create components/card-news/CardNewsList.tsx:

1. 'use client' directive

2. Component signature: CardNewsList({ cards, onUpdate }: { cards: CardNewsItem[]; onUpdate: (cards: CardNewsItem[]) => void })

3. Import types: CardNewsItem from '@/components/lib/types'
4. Import components: Card, CardHeader, CardTitle, CardContent, Badge, Textarea from '@/components/ui'

5. Internal state for editing (optional): editingCardIndex: number | null (default null)

6. Render:
   - Section header with title "카드뉴스"
   - Div container with className="space-y-4"
   - Map over cards array (sorted by order), for each card (with index):
     - Card component with className="border-l-4 border-l-primary/30"
     - CardHeader:
       - Flex container with items-center justify-between
       - CardTitle with className="text-lg":
         - Badge with variant="outline":
           - Text based on card.type: "표지" for cover, "마무리" for cta, "본문 {order number}" for body
     - CardContent with className="space-y-3":
       - Label container with text-sm font-medium and text "헤드라인"
       - Textarea:
         - value: card.headline
         - onChange: update cards state with new headline
         - className: "mt-1 min-h-[60px]"
         - placeholder: "헤드라인을 입력하세요"
       - Label container with text-sm font-medium and text "서브텍스트"
       - Textarea:
         - value: card.subtext
         - onChange: update cards state with new subtext
         - className: "mt-1 min-h-[120px]"
         - placeholder: "서브텍스트를 입력하세요"

7. Update handler: When textarea onChange fires, create new cards array with updated card at index, call onUpdate(newCards)

8. Handle empty cards: Display message "카드뉴스가 없습니다" if cards array is empty

This component implements the list view with editable cards per user decision (RSCH-06: 헤드라인 1줄 + 서브텍스트 2~3줄).</action>
  <verify>File exists at components/card-news/CardNewsList.tsx with CardNewsList component. Run: grep -E "export.*function CardNewsList|export const CardNewsList" components/card-news/CardNewsList.tsx</verify>
  <done>CardNewsList component created with editable cards in list view</done>
</task>

<task type="auto">
  <name>Task 3: Integrate results components into card-news page</name>
  <files>app/card-news/page.tsx</files>
  <action>Update app/card-news/page.tsx:

1. Import new components:
   - ResearchResults from '@/components/card-news/ResearchResults'
   - CardNewsList from '@/components/card-news/CardNewsList'

2. Add new state for editable cards:
   - cards: CardNewsItem[] (initialized as empty array)

3. Update onGenerate handler:
   - Set cardNewsData state
   - Extract cards from response.cards and set cards state
   - This allows separate state for editing

4. Add onUpdateCards handler:
   - Update cards state when user edits card text

5. In render, replace placeholder section with:
   - ResearchResults component (only if cardNewsData exists):
     - sources prop: cardNewsData.researchSources
   - CardNewsList component (only if cards.length > 0):
     - cards prop: cards state
     - onUpdate prop: onUpdateCards handler

6. Add section dividers with horizontal lines between sections for visual separation

7. Use flex/grid layout for better responsive design on larger screens

This completes the main page with full results display and editing functionality.</action>
  <verify>
    <automated>File app/card-news/page.tsx imports and uses ResearchResults and CardNewsList. Run: grep -E "import.*ResearchResults|import.*CardNewsList" app/card-news/page.tsx</automated>
  </verify>
  <done>card-news page updated with ResearchResults and CardNewsList components integrated</done>
</task>

</tasks>

<verification>
1. components/card-news/ResearchResults.tsx exists and displays sources in accordion
2. components/card-news/CardNewsList.tsx exists and displays cards in list view
3. ResearchResults shows badge with source number and title in trigger
4. ResearchResults shows summary and "원문 보기" link in content
5. CardNewsList shows badge with card type (표지/본문/마무리)
6. CardNewsList displays headline and subtext in editable textareas
7. Textarea updates trigger state changes and call onUpdate
8. app/card-news/page.tsx imports and renders both components
9. Results sections only display when data is available
10. Card types are correctly identified and labeled
</verification>

<success_criteria>
- ResearchResults displays all sources with expandable accordion
- Each source shows title, summary, and clickable link to original
- CardNewsList displays all cards in order (cover -> body -> cta)
- Each card has badge showing correct card type
- Headline and subtext are editable via Textarea components
- Changes to textareas update parent state via onUpdate callback
- Results only display after successful generation
- UI follows shadcn/ui patterns and Tailwind classes
- Korean language used throughout the interface
</success_criteria>

<output>
After completion, create `.planning/phases/01-research-copy-generation/01-03-SUMMARY.md`
</output>

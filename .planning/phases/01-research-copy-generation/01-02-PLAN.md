---
phase: 01-research-copy-generation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - components/card-news/ResearchForm.tsx
  - components/card-news/ApiKeyInput.tsx
  - app/card-news/page.tsx
  - app/page.tsx
autonomous: true
requirements:
  - RSCH-01
  - UI-04
user_setup: []
must_haves:
  truths:
    - "User can enter topic and target audience in a form"
    - "User can input Claude API key securely"
    - "Generate button triggers the research and copy generation flow"
  artifacts:
    - path: "components/card-news/ResearchForm.tsx"
      provides: "Input form for topic and audience"
      exports: ["ResearchForm"]
    - path: "components/card-news/ApiKeyInput.tsx"
      provides: "API key input component with state management"
      exports: ["ApiKeyInput"]
    - path: "app/card-news/page.tsx"
      provides: "Main card news generation page"
      exports: ["default"]
  key_links:
    - from: "components/card-news/ResearchForm.tsx"
      to: "app/card-news/page.tsx"
      via: "Component import and props"
      pattern: "import.*ResearchForm.*from.*@/components/card-news/ResearchForm"
    - from: "components/card-news/ApiKeyInput.tsx"
      to: "app/card-news/page.tsx"
      via: "Component import and props"
      pattern: "import.*ApiKeyInput.*from.*@/components/card-news/ApiKeyInput"
    - from: "app/card-news/page.tsx"
      to: "/api/anthropic"
      via: "fetch call on form submit"
      pattern: "fetch.*api/anthropic"
---

<objective>
Create input form components and main card news generation page.

Purpose: Build the user interface layer for data collection - topic input, target audience input, and API key input.
Output: ResearchForm component, ApiKeyInput component, and main card-news page with form integration.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-research-copy-generation/01-CONTEXT.md
@.planning/phases/01-research-copy-generation/01-RESEARCH.md
@.planning/phases/01-research-copy-generation/01-01-PLAN.md
</context>

<interfaces>
<!-- Key types from 01-01-PLAN.md that this plan consumes -->

From components/lib/types.ts:
```typescript
export interface GenerateRequest {
  topic: string;
  audience: string;
  apiKey: string;
}

export interface CardNewsResponse {
  cards: CardNewsItem[];
  researchSources: ResearchSource[];
}
```

<!-- Existing shadcn/ui components to use -->

From components/ui/input.tsx:
```typescript
export { Input } from "@/components/ui/input"
// Props: className, type, placeholder, value, onChange
```

From components/ui/textarea.tsx:
```typescript
export { Textarea } from "@/components/ui/textarea"
// Props: className, placeholder, value, onChange, disabled
```

From components/ui/button.tsx:
```typescript
export { Button } from "@/components/ui/button"
// Props: className, variant (default, outline, etc.), size, disabled, onClick
```

From components/ui/label.tsx:
```typescript
export { Label } from "@/components/ui/label"
// Props: className, htmlFor, children
```

From components/ui/progress.tsx:
```typescript
export { Progress } from "@/components/ui/progress"
// Props: className, value (0-100), max (default 100)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create ApiKeyInput component</name>
  <files>components/card-news/ApiKeyInput.tsx</files>
  <action>Create components/card-news/ApiKeyInput.tsx:

1. 'use client' directive (needed for useState)

2. Component signature: ApiKeyInput({ value, onChange }: { value: string; onChange: (value: string) => void })

3. Internal state for showPassword: boolean (default false)

4. Render:
   - Label with text "Claude API Key"
   - Input component with:
     - type: showPassword ? 'text' : 'password'
     - value: value prop
     - onChange: event.target.value -> onChange
     - placeholder: "sk-ant-..."
     - className: "font-mono text-sm"
   - Button to toggle password visibility with icon (Eye/EyeOff from lucide-react)

5. Use clsx and cn utilities for className merging

6. Import Button, Input, Label from shadcn/ui components

This component handles secure API key input with visibility toggle.</action>
  <verify>File exists at components/card-news/ApiKeyInput.tsx and exports ApiKeyInput component. Run: grep -E "export.*function ApiKeyInput|export const ApiKeyInput" components/card-news/ApiKeyInput.tsx</verify>
  <done>ApiKeyInput component created with password visibility toggle</done>
</task>

<task type="auto">
  <name>Task 2: Create ResearchForm component</name>
  <files>components/card-news/ResearchForm.tsx</files>
  <action>Create components/card-news/ResearchForm.tsx:

1. 'use client' directive

2. Component signature: ResearchForm({ apiKey, onGenerate }: { apiKey: string; onGenerate: (data: CardNewsResponse) => void })

3. Internal state:
   - topic: string (default '')
   - audience: string (default '')
   - isLoading: boolean (default false)
   - error: string | null (default null)

4. handleSubmit function:
   - Set isLoading true, error null
   - Validate: topic.trim() and audience.trim() must not be empty
   - If validation fails: set error message and return
   - Call fetch('/api/anthropic') with POST method
   - Body: JSON.stringify({ topic, audience, apiKey })
   - Headers: { 'Content-Type': 'application/json' }
   - If response.ok: parse JSON, call onGenerate(data)
   - If error: set error message
   - Finally: set isLoading false

5. Render form:
   - Header with title "카드뉴스 생성"
   - Two input groups:
     a. Topic input (Textarea):
        - Label: "주제"
        - Placeholder: "예: 건강한 식습관의 중요성"
        - value: topic state
        - onChange: update topic
     b. Audience input (Textarea):
        - Label: "타깃 독자"
        - Placeholder: "예: 30대 직장인, 건강에 관심 있는 일반인"
        - value: audience state
        - onChange: update audience
   - Generate button:
     - Text: isLoading ? "생성 중..." : "생성하기"
     - Disabled: isLoading or !topic.trim() or !audience.trim()
     - OnClick: handleSubmit
   - Progress component when isLoading is true
   - Error display when error state exists

6. Import Textarea, Button, Label, Progress from shadcn/ui

This is the main form that collects user input and triggers the API call.</action>
  <verify>File exists at components/card-news/ResearchForm.tsx with ResearchForm component. Run: grep -E "export.*function ResearchForm|export const ResearchForm" components/card-news/ResearchForm.tsx</verify>
  <done>ResearchForm component created with topic, audience inputs and generate button</done>
</task>

<task type="auto">
  <name>Task 3: Create card-news page and update home page</name>
  <files>app/card-news/page.tsx, app/page.tsx</files>
  <action>Create app/card-news/page.tsx:

1. 'use client' directive

2. Internal state:
   - apiKey: string (default '')
   - cardNewsData: CardNewsResponse | null (default null)

3. Import types: CardNewsResponse from '@/components/lib/types'
4. Import components: ResearchForm, ApiKeyInput from '@/components/card-news'

5. Render layout:
   - Header section with title "AI 카드뉴스 자동 제작"
   - ApiKeyInput component for API key
   - ResearchForm component with:
     - apiKey prop
     - onGenerate prop that sets cardNewsData state
   - Placeholder section for results (to be filled in Plan 03)

6. Update app/page.tsx:
   - Add navigation link to /card-news
   - Add button with text "카드뉴스 만들기 시작하기"

7. Import Button component from shadcn/ui

This creates the main page with form integration and navigation from home.</action>
  <verify>
    <automated>File app/card-news/page.tsx exists. Run: ls -la app/card-news/page.tsx</automated>
  </verify>
  <done>card-news page created with ResearchForm and ApiKeyInput integration. Home page updated with navigation link.</done>
</task>

</tasks>

<verification>
1. components/card-news/ApiKeyInput.tsx exists and exports ApiKeyInput component
2. components/card-news/ResearchForm.tsx exists and exports ResearchForm component
3. app/card-news/page.tsx exists and uses both components
4. app/page.tsx has navigation link to /card-news
5. Form has proper validation (topic and audience required)
6. Generate button calls /api/anthropic endpoint on submit
7. Loading state disables button and shows progress indicator
8. Error state displays error message to user
</verification>

<success_criteria>
- ApiKeyInput component works with password visibility toggle
- ResearchForm component accepts topic and audience inputs
- Form validation prevents empty submissions
- Generate button triggers API call to /api/anthropic
- Loading and error states are properly displayed
- Navigation from home page to card-news page works
- API key is passed from parent state to form component
</success_criteria>

<output>
After completion, create `.planning/phases/01-research-copy-generation/01-02-SUMMARY.md`
</output>
